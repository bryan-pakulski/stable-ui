version: '3.3'

services:
  stable-diffusion:
    image: "sd"
    container_name: sd
    tty: true 
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env_docker
    volumes:
      - /run/media/bryanp/Dev/projects/public/Stable Diffusion/ultimate-stable-diffusion:/sd
      - /run/media/bryanp/Dev/projects/public/Stable Diffusion/stable-diffusion-weights:/models
      - ../output:/output
      - conda_env:/opt/conda
      - root_profile:/root
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
volumes:
  conda_env:
  root_profile:
